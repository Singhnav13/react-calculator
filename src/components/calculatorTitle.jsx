import React from 'react';

const CalculatorTitle = (props) => {
    return (
        <div className="claculator-title">
            { props.value }
        </div>
    )
}

export default CalculatorTitle;